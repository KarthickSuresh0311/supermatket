# Marutham Market

A responsive, interactive supermarket demo for Tamil Nadu. It is built with vanilla HTML, CSS, and JavaScript, so it has no installation step.

Open `index.html` in a browser to run it locally.

Features include product search, category filtering, quick product views, a persistent shopping basket, location selection, login and checkout demo flows, responsive layouts, and newsletter feedback.
